import 'package:flutter/cupertino.dart';

/// It's a class that contains a static RouteObserver
class Helper {
  /// A RouteObserver that is used to observe the routes.
  static final RouteObserver<PageRoute> routeObserver = RouteObserver();
}
